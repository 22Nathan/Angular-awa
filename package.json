{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json"
  },
  "dependencies": {
    "@angular/cli": "^14.0.1",
    "json-server": "^0.17.0"
  }
}
